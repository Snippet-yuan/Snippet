<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.snippet.auth.mapper.UserAccountMapper">
    <select id="selectByUsername" resultType="com.snippet.auth.entity.UserAccount">
        SELECT
            id,
            username,
            password_hash,
            avatar_asset_id,
            status,
            token_version,
            created_at,
            updated_at
        FROM user_account
        WHERE username = #{username}
        LIMIT 1
    </select>

    <select id="selectById" resultType="com.snippet.auth.entity.UserAccount">
        SELECT
            id,
            username,
            password_hash,
            avatar_asset_id,
            status,
            token_version,
            created_at,
            updated_at
        FROM user_account
        WHERE id = #{userId}
        LIMIT 1
    </select>

    <select id="selectTokenStateById" resultType="com.snippet.auth.entity.UserAccount">
        SELECT
            id,
            status,
            token_version
        FROM user_account
        WHERE id = #{userId}
        LIMIT 1
    </select>

    <insert id="insert"
            useGeneratedKeys="true"
            keyProperty="id"
            keyColumn="id">
        INSERT INTO user_account
            (username, password_hash, status)
        VALUES
            (#{username}, #{passwordHash}, #{status})
    </insert>

    <update id="updatePassword">
        UPDATE user_account
        SET password_hash = #{passwordHash},
            token_version = token_version + 1
        WHERE id = #{userId}
    </update>
</mapper>
