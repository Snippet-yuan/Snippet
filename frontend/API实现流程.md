第 1 步：确定接口契约
开工前先明确：请求路径、方法、请求体字段、响应格式、错误码约定。这是所有代码的"合同"，定了它后续才不会被返工。

第 2 步：写 mock（如果后端没就绪）
把接口契约变成"可执行的模拟版"。你的做法是对的——mock 放独立目录（src/mock/），只 mock 数据，不动业务代码。这样等真后端好了，删掉 mock 拦截层即可无缝切换。

关键点：mock 和真实接口的契约必须一致（URL、字段名、错误码），否则联调时全是坑。

第 3 步：写 API 请求层（api/）
封装 fetch/axios，统一处理：

请求头（token、Content-Type）
非 2xx 和业务码错误
返回值规范
组件里不出现任何 fetch 字样。

第 4 步：写状态管理（stores/）
登录状态、token 持久化、登出清理。页面跳转、导航栏显示用户名等全局逻辑都从这里读，避免每个组件自己存一份。

第 5 步：写校验（utils/ 或 composables）
纯函数校验规则，登录注册共用。这一步什么时候写都行，因为它不依赖任何东西——放第 2 步之后也行，纯看心情。

第 6 步：写页面组件（最后）
组件只做三件事：v-model 收集输入 → 调用 store/api → 展示结果（错误、loading）。它是最容易变的一层（UI 改来改去），放最后写，前面的层稳定了它接上去就快了。

第 7 步：全局配置收尾
main.js 注册 Pinia、路由守卫（未登录跳 /login）、mock 环境开关。
